name: flutter_web_router
description: A router that handles request URIs with wildcards and generate URIs with parameters.
version: 0.0.1+2
homepage: https://github.com/keyber-inc/flutter_web_router

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
