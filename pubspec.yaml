name: flutter_web_router
description: A router that handles request URIs with wildcards.
version: 0.0.1
homepage: https://github.com/keyber-inc/flutter_web_router

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
